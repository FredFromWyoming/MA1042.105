#Ejercicio 2: Defina una función llamada maximo, que dados dos números, regrese el mayor de los dos (SIN USAR LA FUNCIÓN max).
def maximo (a,b):
    if (a>b):
        return (a)
    elif (b>a):
        return (b)
    else:
        return (0)